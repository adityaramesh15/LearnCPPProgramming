#include <iostream>

int main() {
    /*
        const — declared constant
        constexpr — constant expressions  
        enum — enumerated expressions
        #define -  defined constants (Don't use this anymore, no type checking protection).
    */

    const int month = 12;
    
    return 0; 
}